// TODO: need to turn this into a song event, not a script
function beatHit(curBeat) {
	switch (curBeat) {
		case 16:
			game.camZooming = true;
			game.gf.danceSpeed = 0.5;
		case 48:
			game.gf.danceSpeed = 1;
		case 80:
			game.gf.danceSpeed = 0.5;
		case 112:
			game.gf.danceSpeed = 1;
	}
}